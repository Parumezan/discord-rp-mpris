all:
	# @[ -d ./env ] || (echo "Create Virtual Environment..." && virtualenv -q env \
	# && echo "Install Pip Dependencies..." && pip install -qr requirements.txt)
	# python3 src/${RUNFILE}

install:

uninstall:

.PHONY: all install uninstall